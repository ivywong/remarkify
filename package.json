{
  "name": "remarkify",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "compile": "bun build --compile index.ts --outfile dist/remarkify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
